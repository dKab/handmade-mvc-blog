{% if user is defined %}
  {% set controller = 'admin' %}
{% else %}
  {% set controller = 'index' %}
{% endif %}
<!DOCTYPE html>
<html>
    <head>
        <title>
{% block title title|default('Привет!') %}
- Алёнин блог
        </title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width">
        <style type="text/css">
            #nav li {
                display: inline;
            }
        </style>
    </head>
    <body>
        <div id="nav">
                <ul style="list-style: none">
                    <li><a href="{{ "/" ~ controller }}">Все записи</a></li> | 
                    {% if user is defined %}
                        <li><a href="/admin/add">Добавить запись</a></li> | 
                        <li><a href="manageposts">Управление записями</a></li> | 
                        <li><a href="/admin/logout">Выход</a></li>
                    {% else %}
                        <li><a href="/index/login">Вход</a></li>
                    {% endif %}
                </ul>
        </div>
        <div id="content">
            {% if user is not defined %}
            Приветствую вас в своём уберкрутом блоге!
            {% endif %}
            
            {% if header is defined %}
                <h2>{{ header }}</h2>
            {% endif %}
                
            {% block content %}
            {% endblock %}
            
            {% if feedback is defined %}
            {{ feedback }}
            {% endif %}
        </div>
        <div id="sidebar">
         </div>
        </div>
    </body>
</html>
